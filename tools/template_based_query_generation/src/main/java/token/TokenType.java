package token;

public enum TokenType {
  table_name,
  table_schema,
  partition_exp,
  cluster_exp,
  insert_exp,
  values_exp,
  update_item,
  condition,
  select_exp,
  from_item,
  group_exp,
  order_exp,
  count,
  skip_rows,
}
